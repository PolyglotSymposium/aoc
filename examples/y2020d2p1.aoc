list
  at ./y2020d2.txt
  separated by '\n'
  where each parses as
    {min:num}-{max:num} {c:char}: {phrase:text}

solution
  (min x) <= (count_char (c x) (phrase x)) && (count_char (c x) (phrase x)) <= (max x)
    | count
