list
  at ./y2020d2.txt
  separated by '\n'
  where each parses as
    {min:num}-{max:num} {c:char}: {phrase:text}

solution
  ((base_one_char_at (min x) (phrase x)) = c x) .^. ((base_one_char_at (max x) (phrase x)) = c x)
   | count
