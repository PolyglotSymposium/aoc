graph at ./y2020d7.txt
  preprocess
    strip "bags"
    strip "bag"
    strip "."
    strip "no other"
  where
    edges are denoted by "contain"
    left node parses as {bigger:text}
    right nodes are separated by "," and parse as {amount:num} {smaller:text}
  builds graph
    from smaller to bigger

solution
  reachable_from "shiny gold"
    | count