conway of 2 dimensions
  initial state at ./y2019d24.txt
  where '#' means 'bug'
  and   '.' means 'empty'
  cells transition
    from bug to bug if (adjacent bug) = 1
    from empty to bug if (adjacent bug) = 1 || (adjacent bug) = 2
    otherwise a cell is empty

solution
  first_repeated_generation
    | positions bug
    | reading_order
    | 2 ^ x
    | sum
