graph at ./y2019d6.txt
  where
    edges are denoted by ")"
    left node parses as {object:text}
    right node parses as {orbiter:text}
  builds graph
    from object to orbiter

solution
  dijkstra_from "COM"
    | raw_distances
    | sum