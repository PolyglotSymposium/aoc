module Context
       ( identType
       , identValue
       , Context
       , add
       , fromList
       ) where

import qualified Data.Map.Strict as M
import qualified Type
import qualified Value

